{
  "$schema": "https://wopr.dev/schema/config.json",
  "name": "Automation Configuration",
  "description": "Configuration for automated workflows with extended timeout. YOLO mode is enabled by default in the Kimi provider.",
  "version": "1.0.0",

  "session": {
    "name": "kimi-automation",
    "provider": "kimi",
    "model": "kimi-k2",
    "workDir": "."
  },

  "warnings": [
    "YOLO mode is enabled by default - the AI can make filesystem changes without confirmation",
    "Only use this configuration in version-controlled environments",
    "Review changes regularly with git diff"
  ],

  "metadata": {
    "created": "2024-01-15",
    "author": "WOPR User",
    "tags": ["advanced", "automation"],
    "requires": {
      "git": true,
      "versionControl": "highly recommended"
    },
    "notes": "Use --system flag to pass system prompts when sending queries"
  }
}
